@model IList<BalanceSheet.Models.BookingViewModels>

@{
    var ItemCount = 0;
}

<table class="table table-bordered table-hover">
    <tr>
        <th>#</th>
        <th>@Html.DisplayNameFor(m => Model.First().Kind)</th>
        <th>@Html.DisplayNameFor(m => Model.First().Date)</th>
        <th>@Html.DisplayNameFor(m => Model.First().Amount)</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@(++ItemCount)</td>
            <td>@Html.DisplayFor(m => item.Kind)</td>
            <td>@Html.DisplayFor(m => item.Date)</td>
            <td>@Html.DisplayFor(m => item.Amount)</td>
        </tr>
    }
</table>

